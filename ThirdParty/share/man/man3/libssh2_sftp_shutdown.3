.\" generated by cd2nroff 0.1 from libssh2_sftp_shutdown.md
.TH libssh2_sftp_shutdown 3 "2026-03-16" libssh2
.SH NAME
libssh2_sftp_shutdown \- shut down an SFTP session
.SH SYNOPSIS
.nf
#include <libssh2.h>
#include <libssh2_sftp.h>

int
libssh2_sftp_shutdown(LIBSSH2_SFTP *sftp);
.fi
.SH DESCRIPTION
\fIsftp\fP \- SFTP instance as returned by libssh2_sftp_init(3)

Destroys a previously initialized SFTP session and frees all resources
associated with it.
.SH RETURN VALUE
Return 0 on success or negative on failure. It returns
LIBSSH2_ERROR_EAGAIN when it would otherwise block. While
LIBSSH2_ERROR_EAGAIN is a negative number, it is not really a failure per se.
.SH SEE ALSO
.BR libssh2_sftp_init(3)
